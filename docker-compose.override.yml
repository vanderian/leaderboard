version: '3'

services:
  api:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:5000
    network_mode: host
#    ports:
#      - "5000:5000"
  silo:
    network_mode: host
#    ports:
#      - "8080:8080"
#      - "30000:30000"
